stages:
- stage: BuildShell
  dependsOn: BuildStg
  jobs: 
  - job: build_file
    variables: 
      test: $[ stageDependencies.BuildStg.build.outputs['test.test'] ]
    steps:
    - script: |
        mv linux-out/scripts/test.sh linux-out/acrconnect-installer-cli-$(test)[linux-x64].sh
        sed -i 's/aivversion/$(test)/g' linux-out/acrconnect-installer-cli-$(test)[linux-x64].sh
        
    - task: PublishBuildArtifacts@1
      displayName: 'Publish Artifact'
      inputs:
        PathtoPublish: 'linux-out'
        artifactName: 'acrconnect-installer-cli-$(test)[linux-x64]'
