trigger:
- master

stages:
- stage: BuildStg
  jobs:
  - job: build
    pool:
      vmImage: 'ubuntu-18.04'
    steps:
    - script: |
        div=1.0.0.0
        echo "##vso[task.setvariable variable=div;isOutput=true]$div"
    - script: echo $(div)
      
  - job: build_2
    dependsOn: build
    variables:
      aiv: $[ dependencies.build.outputs['div'] ]
    steps:
    - script: echo "aiv=$(aiv)"
